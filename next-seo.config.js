const title = 'Tchorek Shop';
const description = 'Najlepszy sklep... Tchorek Shop';

const nextSeoConfig = {
  title,
  description,
  openGraph: {
    title,
    description,
    siteName: 'Tchorek Shop',
  },
};

export default nextSeoConfig;
